/**
 * Tiny jQuery plugin which iterates through elements in reverse order,
 * as per http://stackoverflow.com/questions/1394020/jquery-each-backwards
 *
 * $('jquery-selectors-go-here').reverse().each(function () {
 *  //business as usual goes here
 * });
 */

jQuery.fn.reverse = [].reverse
